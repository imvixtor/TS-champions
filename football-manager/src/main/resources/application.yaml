spring:
  application:
    name: football-manager

  datasource:
    url: jdbc:mysql://localhost:3306/football_db?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: "@Acctonvn12345" # Mật khẩu của bạn
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

application:
  security:
    jwt:
      secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
      expiration: 86400000

  file:

    upload-dir: "D:/uploads/football"